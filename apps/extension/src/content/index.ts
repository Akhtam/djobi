/**
 * Content script injected on ATS host pages (`manifest.ts` → `content_scripts`). Will detect the
 * job posting, scrape its text, classify form fields, and fill the form — currently an empty stub,
 * see `PROGRESS.md` Phase 6.
 */
export {};
