/**
 * MV3 background service worker (`manifest.ts` → `background.service_worker`). Will relay
 * `chrome.runtime.onMessage` calls from the popup/content script to `callBackend.ts` — currently an
 * empty stub, see `PROGRESS.md` Phase 5.
 */
export {};
