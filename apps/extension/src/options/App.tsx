/** Options page root — placeholder; will hold the profile onboarding form (`PROGRESS.md` Phase 5). */
export function App() {
  return (
    <main>
      <h1>djobi — Profile</h1>
      <p>Profile onboarding form coming soon.</p>
    </main>
  );
}
