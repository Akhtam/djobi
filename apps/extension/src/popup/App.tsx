/**
 * Popup root — placeholder; will hold the review/edit UI for a detected job's tailored resume and
 * drafted answers, plus the "Fill form" action (`PROGRESS.md` Phase 5).
 */
export function App() {
  return (
    <main>
      <h1>djobi</h1>
      <p>Navigate to a supported job application page to get started.</p>
    </main>
  );
}
