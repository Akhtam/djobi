# The backend's origin, read by `src/extensionConfig.ts`. Unlike the dashboard, this is the real
# base URL every extension request is sent to — extension pages have no web origin of their own to
# proxy through. Leave unset locally; set to the real deployed backend's origin before packaging a
# production build.
VITE_BACKEND_ORIGIN=http://127.0.0.1:5391
